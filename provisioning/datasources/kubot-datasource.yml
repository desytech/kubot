apiVersion: 1
datasources:
  - name: kubot-datasource
    type: postgres
    access: proxy
    url: host.docker.internal:5433
    password: kubot
    user: kubot
    database: kubot
    basicAuth: false
    jsonData:
      sslmode: disable
    editable: true
    isDefault: true